import 'package:flutter/material.dart';

/// FairF brand palette — yellow + cyan on dark, matching the logo/theme.
class FairFColors {
  static const Color background = Color(0xFF14171C);
  static const Color surface = Color(0xFF1E232B);
  static const Color yellow = Color(0xFFFFD53D);
  static const Color cyan = Color(0xFF33E1FF);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAB2BD);
}
